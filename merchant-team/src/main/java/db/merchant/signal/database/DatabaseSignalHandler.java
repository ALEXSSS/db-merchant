package db.merchant.signal.database;

import db.merchant.signal.SignalHandler;

public class DatabaseSignalHandler implements SignalHandler {
    @Override
    public void handleSignal(int signal) {

    }
}
