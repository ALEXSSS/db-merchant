package db.merchant.signal.codebase;

import db.merchant.signal.SignalHandler;

public class CodeBasedSignalHandler implements SignalHandler {

    @Override
    public void handleSignal(int signal) {

    }
}
